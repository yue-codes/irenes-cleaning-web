---
interface Props {
  class?: string
  target?: string
  url: string
}

const { url, target, class: className } = Astro.props
---

<a
  href={url}
  target=`${target ?? '_self'}`
  rel="noopener noreferrer"
  class:list={[
        className,
        "inline-flex",
        "items-center",
        "justify-center",
        "rounded-full",
        "border",
        "border-purple-600",
        "px-6",
        "py-3",
        "text-sm",
        "font-medium",
        "text-white", // Texto blanco por defecto
        "bg-purple-600", // Fondo rosa por defecto
        "hover:bg-white", // Fondo blanco al pasar el cursor
        "hover:text-purple-600", // Texto rosa al pasar el cursor
        "focus:outline-none",
        "focus:ring",
        "active:bg-white", // Fondo blanco al hacer clic
        "active:text-purple-600", // Texto rosa al hacer clic
        "whitespace-nowrap",
        "gap-2",
  ]}
>
  <slot />
</a>
